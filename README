nasm -g -f elf32 fisier.asm
gcc -m32 fisier.o -o exec

objdump -M intel -d fisierObiect.o
python -c 'print("HAi la mare")'
popcnt	unde_afiseaza, unde_verifica 







%include "io.inc"
section .data
    
    
section .text
global CMAIN
CMAIN:
    push ebp
    mov ebp, esp

    # MAIN

