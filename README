nasm -g -f elf32 fisier.asm
gcc -m32 fisier.o -o exec

objdump -M intel -d fisierObiect.o








%include "io.inc"
section .data
    
    
section .text
global CMAIN
CMAIN:
    push ebp
    mov ebp, esp

    # MAIN

